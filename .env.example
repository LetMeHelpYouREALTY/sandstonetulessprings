# Canonical production origin — must match your Google Search Console property (www vs apex).
NEXT_PUBLIC_BASE_URL=https://www.sandstonetulessprings.com

# Google Search Console — HTML tag verification (Search Console → Settings → Ownership verification).
# NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=your-verification-code-here

# Optional ISO date for sitemap lastmod (e.g. 2026-05-19) — set in Vercel on production deploys.
# NEXT_PUBLIC_BUILD_DATE=2026-05-19

# Public contact email — must match GBP when email is listed on the profile.
# NEXT_PUBLIC_SITE_EMAIL=drjansells@sandstonetulessprings.com

# Calendly — secondary conversion (Follow Up Boss via native integration + UTM)
# 30-min private tour (primary page CTAs):
# NEXT_PUBLIC_CALENDLY_TOUR_URL=https://calendly.com/your-event/tour
# 15-min conversation (floating badge):
# NEXT_PUBLIC_CALENDLY_CONSULTATION_URL=https://calendly.com/your-event/consultation
# Optional fallback if only one event type is configured:
# NEXT_PUBLIC_CALENDLY_URL=

# RealScout — primary conversion (office listings on every page; script loads once in layout)
# NEXT_PUBLIC_REALSCOUT_AGENT_ID=QWdlbnQtMjI1MDUw
# Optional shared-search URL for text CTAs (per-site; not the /onboarding link):
# NEXT_PUBLIC_REALSCOUT_URL=

# Google Business Profile — this site only (Sandstone Tules Springs | Homes By Dr. Jan Duffy)
# Phone must match the call-tracking number on the GBP listing (default in site-contact: +17024661509).
# NEXT_PUBLIC_GBP_PHONE=+17024661509
# Place ID from Google Place ID Finder — enables review deep links and optional Place embed.
# NEXT_PUBLIC_GBP_PLACE_ID=ChIJ...
# Optional g.page/r/... short link from GBP dashboard for Write Review CTA.
# NEXT_PUBLIC_GBP_WRITEREVIEW_URL=
# Set true on preview envs or while GBP is pending verification (hides review CTAs).
# NEXT_PUBLIC_GBP_REVIEWS_DISABLED=true
# When Place ID embed is preferred, add Maps Embed API key (Vercel env only).
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
# After 5+ verified GBP reviews — integers only; omit until accurate.
# NEXT_PUBLIC_GBP_AGGREGATE_RATING_VALUE=5
# NEXT_PUBLIC_GBP_AGGREGATE_RATING_COUNT=12
